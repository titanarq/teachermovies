<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- The phone app talks to the TV's HTTP API on the LAN (later sub-issues of #35). -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MobileApp"
        android:label="@string/app_name"
        android:theme="@style/Theme.MovieAssistant">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- The share target (#199), offered by any app's share sheet under `@string/app_name`
             ("Movie Assistant"). Exported because it is the platform that starts it. -->
        <activity
            android:name=".share.ShareActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MovieAssistant.Share">
            <!-- `Share -> Movie Assistant` on `text/plain`: a magnet inside shared text. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <!-- Opening a `magnet:` link from a browser or a chat. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="magnet" />
            </intent-filter>
        </activity>
    </application>
</manifest>
